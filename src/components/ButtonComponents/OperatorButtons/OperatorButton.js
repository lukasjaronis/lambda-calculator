import React from "react";
import { operators } from "../../../data";

export const OperatorButton = props => {
  return (
    <>
    <button>
    {props.operate.char}
    </button>
     
    </>
  );
};
